tags:
  - Posts
operationId: create-an-post
summary: Create an Post
description: Create an Post
security:
  - userApiKey: []
parameters:
  - name: data
    in: body
    required: true
    schema:
      $ref: '#/definitions/resource_create_payload'
responses:
  200:
    description: Success
    schema:
      $ref: '#/definitions/resource_create_payload'
  401:
    description: Unauthorized
    schema:
      $ref: '#/definitions/bad_request_error'
